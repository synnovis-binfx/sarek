/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Config file for defining DSL2 per module options and publishing paths
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Available keys to override module options:
        ext.args   = Additional arguments appended to command in module.
        ext.args2  = Second set of arguments appended to command in module (multi-tool modules).
        ext.args3  = Third set of arguments appended to command in module (multi-tool modules).
        ext.prefix = File name prefix for output files.
        ext.when   = When to run the module.
----------------------------------------------------------------------------------------
*/

// VARDICT
// currently for tumor only as pipeline fall-back and for validation purposes

process {
    if (params.tools && params.tools.split(',').contains('vardict')) {

        withName: 'VARDICTJAVA' {
            ext.args   = { "-f 0.02 -z -c 1 -S 2 -E 3 -g 4 -I 200 -P 0 -X 1 -Q 10 --mfreq 0.15 " }
            ext.args2  = { "-A -f 0.02 -P 0 -p 1 -I 15 " }
            ext.prefix = { meta.num_intervals <= 1 ? "${meta.id}.vardict" : "${meta.id}.vardict.${intervals.baseName}" }
            ext.when   = { params.tools && params.tools.split(',').contains('vardict') }
            publishDir = [
                mode: params.publish_dir_mode,
                path: { "${params.outdir}/variant_calling/" },
                pattern: "*{vcf.gz,vcf.gz.tbi}",
                saveAs: { meta.num_intervals > 1 ? null : "vardict/${meta.id}/${it}" }
            ]
        }

    }
}
